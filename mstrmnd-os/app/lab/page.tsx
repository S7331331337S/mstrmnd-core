import { LabCanvas } from "./lab-canvas";

export const metadata = {
  title: "Lab — MSTRMND",
  description: "ASCII / dither experiment canvas — platinum on obsidian",
};

export default function LabPage() {
  return <LabCanvas />;
}
