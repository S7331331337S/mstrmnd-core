import { FieldCanvas } from "./field-canvas";

export const metadata = {
  title: "Field — MSTRMND",
  description: "Platinum field — vgpu WebGPU with WebGL2 fallback",
};

export default function FieldPage() {
  return <FieldCanvas />;
}
