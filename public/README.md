# public/

Static output root for the Vercel `mstrmnd-core` project (Output Directory = `public`).

The monorepo `build` script is typecheck-only (`tsc --noEmit`); this folder is the deployable artifact Vercel expects after build.
